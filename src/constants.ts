export const MAX_DEPTH = 2;

export const DEFAULT_VERSION_INCREMENT = 1;
export const DEFAULT_VERSION = 0;

export const DEFAULT_CONFIG = {
  schemaPath: 'http://localhost:6200/graphql',
  outputDir: '__tests__/api/tests/generated/',
  mockDir: '__tests__/api/mocks/',
  depth: 4,
};
